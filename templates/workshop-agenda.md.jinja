# Workshop Agenda: Modeling Readiness Review

**Workspace**: {{ workspace_name }}
**Run ID**: {{ run_id }}
**Assessment Date**: {{ assessment_date }}
**Recommended duration**: 2.5 hours

---

## Pre-Work (Before the Session)

- [ ] Distribute [Executive Summary](executive-summary.md) to all attendees
- [ ] Review [Findings Detail](findings-detail.md) — data architects and semantic model owners
- [ ] Identify owners for each source system listed in findings

---

## Agenda

### 1. Opening — Context and Scope (15 min)

- What was scanned: {{ artifact_counts.semantic_models }} semantic model(s), {{ artifact_counts.ontologies }} ontology(s)
- Assessment disciplines covered:
  - ✅ Canonical entity modeling
  - ✅ Field-level lineage
  - ⏸ Layered modeling (not assessed in this version)
  - ⏸ Steward-loop modeling (not assessed in this version)
- Scoring methodology: rubric-based, 0–4 scale

---

### 2. Canonical Entity Modeling Deep Dive (45 min)

**Score: {% set cem_score = maturity_scores | selectattr("discipline", "equalto", "canonical_entity_modeling") | first %}{{ cem_score.score_display }} / 4**

{% set cem_findings = findings | selectattr("discipline", "equalto", "canonical_entity_modeling") | list %}
{% if cem_findings %}
Conflicts to discuss:
{% for f in cem_findings %}
- **{{ f.entity_name }}** conflict (`{{ f.finding_id }}`): {{ f.description }}
{% endfor %}

Discussion questions:
- Which definition of each conflicting entity should be treated as authoritative?
- Who owns the canonical entity registry for this workspace?
- What is the target timeline for aligning primary key conventions?
{% else %}
*No canonical entity modeling conflicts detected — this section may be shortened.*
{% endif %}

---

### 3. Field-Level Lineage Deep Dive (45 min)

**Score: {% set fll_score = maturity_scores | selectattr("discipline", "equalto", "field_level_lineage") | first %}{{ fll_score.score_display }} / 4**

{% set fll_findings = findings | selectattr("discipline", "equalto", "field_level_lineage") | list %}
{% if fll_findings %}
Source-attribution gaps to discuss:
{% for f in fll_findings %}
- **{{ f.entity_name }}** (`{{ f.finding_id }}`): {{ f.description }}
{% endfor %}

Discussion questions:
- Which source systems provide extraction timestamps? Which do not?
- How should confidence scores be calculated for each entity type?
- Who is responsible for ontology updates in Fabric IQ?
{% else %}
*No field-level lineage gaps detected — this section may be shortened.*
{% endif %}

---

### 4. Future Disciplines Preview (15 min)

The following disciplines were **not assessed in this version**:

- **Layered modeling** — requires detection of bronze/silver/gold or equivalent staging layers
- **Steward-loop modeling** — requires detection of data stewardship feedback loops

Action: Discuss whether these disciplines are relevant for your workspace and when to enable assessment.

---

### 5. Remediation Planning (30 min)

Review the [Remediation Plan](remediation-plan.md):
- Assign owners for each remediation action
- Agree on target completion dates
- Schedule follow-up scanner run date

---

### 6. Wrap-Up (10 min)

- Confirm action item owners and dates
- Schedule next assessment run
- Identify any additional scope (additional workspaces, disciplines)

---

*Generated by Modeling Readiness Assessor narrator · Run ID: {{ run_id }} · Artifact: {{ artifact_id }}*
